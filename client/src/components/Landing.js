import React from 'react';

const Landing = () => {
  return (
    // Two sets of braces (first to indicate JS & second for styling obj)
    <div style={{ textAlign: 'center' }}>
      <h1>Email Surveys</h1>
      Collect feedback from your users.
    </div>
  );
};

export default Landing;
